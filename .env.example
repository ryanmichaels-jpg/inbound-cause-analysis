# Anthropic API key — required by the insight layer (`python -m ica.insight`,
# the CP4 resonance extraction + GTM artifact generation). Not used by the
# generator, the test suite, or the dashboard. Copy this file to .env and fill
# in your key; .env is git-ignored.
ANTHROPIC_API_KEY=
